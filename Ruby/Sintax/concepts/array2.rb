arr = Array.new;

puts arr.size();
puts arr.length();
puts arr.count();

=begin
    Qual a diferença entre o método count e size? e Por que e quando usar o count?

    count: retorna o número de elementos do array.
    size: retorna o tamanho do array.
    length: retorna também o tamanho do array.
=end